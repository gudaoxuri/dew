dew:
  basic:
    entity:
      basePackages: ""
  cluster:
    cache: redis
    dist: hazelcast
    mq: hazelcast

spring:
  redis:
    host: localhost
    port: 6379
    database: 0
  hazelcast:
    addresses: ["127.0.0.1"]
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false
