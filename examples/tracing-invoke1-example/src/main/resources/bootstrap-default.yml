spring:
  cloud:
    config:
      enabled: false
    kubernetes:
      ribbon:
        enabled: false
  rabbitmq:
    host: 10.200.20.220
    port: 5672
    username: dew
    password: dew123456

server:
  port: 8081


tracing-invoke2-example:
  ribbon:
    listOfServers: localhost:8082