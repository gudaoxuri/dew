spring:
  application:
    name: sleuth-invoke3-example
  cloud:
    config:
      enabled: false

server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone:  http://registry:123456@localhost:9999/eureka
    eureka-server-read-timeout-seconds: 100