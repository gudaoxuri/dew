spring:
  application:
    name: sleuth-invoke2-example
  cloud:
    config:
      enabled: false

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone:  http://registry:123456@localhost:9999/eureka