spring:
  application:
    name: sleuth-invoke1-example
  cloud:
    config:
      enabled: false

server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone:  http://registry:123456@localhost:9999/eureka