spring:
  application:
    name: cloud-test
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false

eureka:
  client:
    enabled: false

#performance-service.ribbon.listOfServers: localhost:8812