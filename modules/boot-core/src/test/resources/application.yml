dew:
  cluster:
    cache: redis
    dist: redis
    mq: redis

spring:
  application:
    name: core-test
  redis:
    host: localhost
    port: 6379
    database: 0
  hazelcast:
    addresses: ["127.0.0.1"]
  ignite:
    addresses: ["127.0.0.1"]
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test