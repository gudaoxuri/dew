dew:
  cluster:
    election: eureka
    config:
      election-period-sec: 2
spring:
  application:
    name: cluster-eureka-test
  cloud:
    config:
      discovery:
        enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8080/eureka