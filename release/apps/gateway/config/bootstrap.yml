spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      username: config
      password: vim4qGrtbdC7GFr
      label: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://trc:reg!_#123@localhost:9999/eureka