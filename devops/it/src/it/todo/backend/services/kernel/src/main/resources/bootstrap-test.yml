spring:
  cloud:
    config:
      enabled: false
  redis: # 请配置成独立服务以支持多实例
    host: localhost
    port: 6379
    database: 0
  datasource: # 请配置成独立服务以支持多实例
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test